@model List<TeacherSosialMedia>
<!-- Content wrapper -->
<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"> EduHomeEdit /</span> TeacherSosial Tables</h4>

        <!-- Basic Bootstrap Table -->
        <div class="card">
            <h5 class="card-header">Teacher Table<a asp-action="create" class=" btn btn-danger mx-5 "> Create TeacherSosial </a></h5>
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th> Name</th>
                            <th> MediaLink</th>
                            <th>MediaLogo</th>                           
                            <th>Teacher</th>
                        </tr>
                    </thead>
                    @foreach (var teachersosial in Model)
                    {
                        <tbody class="table-border-bottom-0">
                            <tr>
                                <td>@teachersosial.Name</td>
                                <td>@teachersosial.MediaLink</td>
                                <td>@teachersosial.MediaLogo</td>
                                <td>@teachersosial.Teacher.FullName</td>

                                <td>
                                    <div class="dropdown">
                                        <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                            <i class="bx bx-dots-vertical-rounded"></i>
                                        </button>
                                        <div class="dropdown-menu">

                                            <a type="submit" class="dropdown-item" asp-action="Edit" asp-route-id="@teachersosial.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>

                                            <form method="post" asp-action="Delete">
                                                <button type="submit" asp-action="Delete" asp-route-id="@teachersosial.Id" class="dropdown-item"><i class="bx bx-trash me-1"></i> Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </td>
                            </tr>

                        </tbody>
                    }

                </table>
            </div>
        </div>
        <!--/ Basic Bootstrap Table -->


    </div>
    <!-- / Content -->
    <!-- Footer -->



</div>
<!-- Content wrapper -->
