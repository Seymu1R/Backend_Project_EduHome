@model List<Teacher>
<!-- Content wrapper -->
<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"> EduHomeEdit /</span> Teacher Tables</h4>

        <!-- Basic Bootstrap Table -->
        <div class="card">
            <h5 class="card-header">Teacher Table<a asp-action="create" class=" btn btn-danger mx-5 "> Create Teacher </a></h5>
            <div class="table-responsive text-nowrap">
                <table class="table">
                    <thead>
                        <tr>
                            <th>FullName</th>
                            <th>Position</th>
                            <th>Image</th>
                            <th>AboutMe</th>
                            <th>Dgree</th>
                            <th>Expierence</th>
                            <th>Hobbies</th>
                            <th>Faculty</th>
                            <th>Mail</th>
                            <th>Phone</th>
                            <th>SkypeAdress</th>
                            <th>IsDeleted</th>
                        </tr>
                    </thead>
                    @foreach (var teacher in Model)
                    {
                        <tbody class="table-border-bottom-0">
                            <tr>
                                <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@teacher.FullName</strong></td>
                                <td>@teacher.Position</td>
                                <td>
                                    <ul class="list-unstyled users-list m-0 avatar-group d-flex align-items-center">
                                        <li data-bs-toggle="tooltip"
                                        data-popup="tooltip-custom"
                                        data-bs-placement="top"
                                        class="avatar avatar-xs pull-up"
                                        title="Lilian Fuller">
                                            <img src="~/admin/assets/img/teacher/@teacher.ImageUrl" alt="Avatar" class="rounded-circle" />
                                        </li>

                                    </ul>
                                </td>
                                <td><span >@teacher.AboutMe</span></td>
                                <td><span >@teacher.Degree</span></td>
                                <td><span >@teacher.Experience</span></td>
                                <td><span >@teacher.Hobbies</span></td>
                                <td><span >@teacher.Faculty</span></td>
                                <td><span >@teacher.Mail</span></td>
                                <td><span >@teacher.Phone</span></td>
                                <td><span >@teacher.SkypeAdress</span></td>
                                <td><span >@teacher.IsDeleted</span></td>

                                <td>
                                    <div class="dropdown">
                                        <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown">
                                            <i class="bx bx-dots-vertical-rounded"></i>
                                        </button>
                                        <div class="dropdown-menu">

                                            <a type="submit" class="dropdown-item" asp-action="Edit" asp-route-id="@teacher.Id"><i class="bx bx-edit-alt me-1"></i> Edit</a>

                                            <form method="post" asp-action="Delete">
                                                <button type="submit" asp-action="Delete" asp-route-id="@teacher.Id" class="dropdown-item"><i class="bx bx-trash me-1"></i> Delete</button>
                                            </form>
                                        </div>
                                    </div>
                                </td>
                            </tr>

                        </tbody>
                    }

                </table>
            </div>
        </div>
        <!--/ Basic Bootstrap Table -->


    </div>
    <!-- / Content -->
    <!-- Footer -->



</div>
<!-- Content wrapper -->
